const router = new require('koa-router')();

router.get('/index', async (ctx) => {
  ctx.body = 'router -- Hello World!!!';
});

<% if (template) { -%>
router.get('/tpl', async (ctx) => {
  ctx.render('about', { message: 'This is about page!' });
});
<% } -%>

module.exports = router;
